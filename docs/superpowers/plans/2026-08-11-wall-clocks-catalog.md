# Каталог-витрина настенных часов — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship a static, auto-updating product catalog site (wall clocks, same range as sold on Wildberries) with per-product pages, search, and a no-backend order form, deployed free on GitHub Pages and rebuilt daily by GitHub Actions.

**Architecture:** A Node.js build script pulls the whole product catalog (name, description, photo) from the WB Content API and renders fully-formed static HTML (index + one page per product) plus `sitemap.xml`/`robots.txt`. GitHub Actions runs this on a schedule and publishes the output to GitHub Pages. No server, no database, no npm dependencies beyond Node's built-ins.

> **Изменение от 2026-08-13 (обязательно к прочтению перед задачами 5-10):** гугл-таблица убрана из сборки — WB API отдаёт название, описание и фото одним ответом (проверено живым запросом на 300 карточках). Задачи 1 и 2 (`src/sheets.js`, `src/filter.js`) выполнены и затем **удалены** — не восстанавливать. Задача 4 выполнена и переписана как `src/wbCatalog.js` → `fetchAllClockProducts(token, {fetchFn, pageSize})` возвращает готовый `Product[]` `{nmId, name, description, photo}`. Секрет остался один — `WB_API_TOKEN`. Задачи 3, 5, 6, 7, 9, 10 не менялись; задача 8 (`build.js`) — см. исправленный код в самой задаче.

**Tech Stack:** Node.js (>=20, global `fetch`, `node:test`), plain HTML/CSS/vanilla JS (ES modules, no bundler, no framework), GitHub Actions, GitHub Pages.

## Global Constraints

- No new npm dependencies. Use only Node's standard library and global `fetch`. (ponytail: stdlib/native before dependency)
- Tests use Node's built-in `node:test` + `node:assert/strict`. No test framework.
- Contact constants: phone/WhatsApp `+79266642121`, Telegram `@Podarki190`.
- Price is always static and identical for every product: original `7000 ₽` (strikethrough), sale `1890 ₽`, caption "Доставка по всей России Бесплатная". Never read price from the sheet.
- Product page URL pattern: `/tovar/<Артикул WB>/` (nmID as slug — guaranteed unique, no transliteration).
- Catalog inclusion rule: a product is included **only if** the WB Content API returns it under subjectID 625 **and** it has at least one photo.
- Order form fields: ФИО, телефон, город и адрес доставки (free text only — no CDEK/Yandex pickup-point widgets in this phase, see spec).
- Never write the WB API token into any file that gets committed. It is consumed only via environment variable / GitHub Actions secret at build time (locally: gitignored `.env`).
- Spec: `docs/superpowers/specs/2026-08-11-wall-clocks-catalog-design.md` — this plan implements it in full except the explicitly deferred CDEK/Yandex pickup widgets.

---

## File Structure

```
wall-clocks-catalog/
  package.json
  .gitignore
  src/
    links.js          # contact links, price constants, message builders (shared Node+browser)
    wbCatalog.js        # fetch the whole catalog (name/description/photo) from WB Content API
    render.js             # HTML rendering (index page, product page)
    sitemap.js              # sitemap.xml + robots.txt rendering
    build.js                 # orchestrates everything, writes dist/
  test/
    links.test.js
    wbCatalog.test.js
    render.test.js
    sitemap.test.js
    build.test.js
  static/
    style.css
    search.js        # client-side filter on index page
    order-form.js      # client-side order form -> WhatsApp/Telegram
  .github/workflows/
    build-deploy.yml
  docs/superpowers/
    specs/2026-08-11-wall-clocks-catalog-design.md   (already exists)
    plans/2026-08-11-wall-clocks-catalog.md            (this file)
```

`dist/` is generated by the build, never committed (in `.gitignore`).

---

### Задачи 1-4 — ВЫПОЛНЕНО (не переделывать)

- **Задача 1** (`src/sheets.js`) и **Задача 2** (`src/filter.js`) — были сделаны, затем удалены вместе с тестами: гугл-таблица выпилена из сборки (см. «Изменение от 2026-08-13» выше). Код остался в истории git, коммиты `72d7ff2` и `6f940ac`. `package.json` и `.gitignore` из задачи 1 остаются на месте.
- **Задача 3** — `src/links.js` + `test/links.test.js`, 9 тестов, коммит `6b1f519`. Контакты, цены, сборка сообщений WhatsApp/Telegram.
- **Задача 4** — фото и тексты с WB. Форма ответа API проверена живым запросом; итоговый модуль — `src/wbCatalog.js` + `test/wbCatalog.test.js`, 6 тестов: `fetchAllClockProducts(token, {fetchFn, pageSize})` → `Product[]` `{nmId, name, description, photo}`.

**Дальше — с задачи 5.**

---

### Task 5: HTML rendering (index page + product page)

**Files:**
- Create: `src/render.js`
- Test: `test/render.test.js`

**Interfaces:**
- Consumes: product objects `{ nmId: number, name: string, description: string, photo: string }` (как их отдаёт `fetchAllClockProducts`, задача 4); constants/builders from `src/links.js` (Task 3).
- Produces: `renderIndexPage(products: Product[]): string`; `renderProductPage(product: Product): string`; `escapeHtml(str: string): string`. Consumed by `build.js` (Task 8).

- [ ] **Step 1: Write the failing test**

```js
// test/render.test.js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { renderIndexPage, renderProductPage, escapeHtml } from '../src/render.js';

const product = {
  nmId: 1259100136,
  name: 'Часы настенные "Локомотив Ярославль"',
  description: 'А'.repeat(200),
  photo: 'https://basket.wbbasket.ru/x/big/1.jpg',
};

test('escapeHtml escapes quotes and angle brackets', () => {
  assert.equal(escapeHtml(`<b>"Тест" & Co</b>`), '&lt;b&gt;&quot;Тест&quot; &amp; Co&lt;/b&gt;');
});

test('renderIndexPage embeds every product as real HTML text (no JS-only rendering)', () => {
  const html = renderIndexPage([product]);
  assert.match(html, /Локомотив Ярославль/);
  assert.match(html, /1890/);
  assert.match(html, /7000/);
  assert.match(html, /Доставка по всей России Бесплатная/);
  assert.match(html, /loading="lazy"/);
  assert.match(html, /\/tovar\/1259100136\//);
  assert.match(html, /search\.js/);
});

test('renderProductPage sets a per-product title and meta description', () => {
  const html = renderProductPage(product);
  assert.match(html, /<title>Часы настенные &quot;Локомотив Ярославль&quot;/);
  assert.match(html, /<meta name="description" content="[^"]{20,160}"/);
  assert.match(html, /id="order-form"/);
  assert.match(html, /data-nmid="1259100136"/);
  assert.match(html, /order-form\.js/);
});

test('renderProductPage shows the full description, not truncated', () => {
  const html = renderProductPage(product);
  assert.ok(html.includes('А'.repeat(200)));
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `node --test test/render.test.js`
Expected: FAIL — module not found.

- [ ] **Step 3: Implement `src/render.js`**

```js
import {
  ORIGINAL_PRICE, SALE_PRICE, SHIPPING_TEXT,
  buildTelLink, buildWhatsAppLink, buildTelegramLink, buildWbLink,
  buildCardWhatsAppMessage, truncate,
} from './links.js';

export function escapeHtml(str) {
  return String(str)
    .replace(/&/g, '&amp;')
    .replace(/</g, '&lt;')
    .replace(/>/g, '&gt;')
    .replace(/"/g, '&quot;');
}

function renderPriceBlock() {
  return `<div class="price">
    <span class="price-old">${ORIGINAL_PRICE} ₽</span>
    <span class="price-new">${SALE_PRICE} ₽</span>
    <div class="shipping">${SHIPPING_TEXT}</div>
  </div>`;
}

function renderOrderButtons(product) {
  const waMsg = buildCardWhatsAppMessage(product.name, product.nmId);
  return `<div class="order-buttons">
    <a href="${buildTelLink()}">Позвонить</a>
    <a href="${buildWhatsAppLink(waMsg)}" target="_blank" rel="noopener">WhatsApp</a>
    <a href="${buildTelegramLink()}" target="_blank" rel="noopener">Telegram</a>
    <a href="${buildWbLink(product.nmId)}" target="_blank" rel="noopener">Купить на WB</a>
  </div>`;
}

function renderCard(product) {
  const safeName = escapeHtml(product.name);
  return `<article class="card" data-name="${escapeHtml(product.name.toLowerCase())}">
    <a href="/tovar/${product.nmId}/"><img src="${product.photo}" loading="lazy" alt="${safeName}"></a>
    <h3><a href="/tovar/${product.nmId}/">${safeName}</a></h3>
    <p>${escapeHtml(truncate(product.description, 150))}</p>
    ${renderPriceBlock()}
    ${renderOrderButtons(product)}
  </article>`;
}

function pageShell({ title, description, body, scripts = [] }) {
  return `<!doctype html>
<html lang="ru">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>${escapeHtml(title)}</title>
<meta name="description" content="${escapeHtml(description)}">
<link rel="stylesheet" href="/style.css">
</head>
<body>
${body}
${scripts.map(s => `<script type="module" src="${s}"></script>`).join('\n')}
</body>
</html>`;
}

export function renderIndexPage(products) {
  const body = `
<h1>Настенные часы — каталог</h1>
<input id="search" type="search" placeholder="Поиск по названию...">
<div class="grid">
${products.map(renderCard).join('\n')}
</div>`;
  return pageShell({
    title: 'Настенные часы — каталог, бесплатная доставка по России',
    description: `${products.length} моделей настенных часов. Цена ${SALE_PRICE} ₽, доставка по всей России бесплатная.`,
    body,
    scripts: ['/search.js'],
  });
}

export function renderProductPage(product) {
  const safeName = escapeHtml(product.name);
  const body = `
<a href="/">← Ко всем часам</a>
<article class="product">
  <img src="${product.photo}" alt="${safeName}">
  <h1>${safeName}</h1>
  <p>${escapeHtml(product.description)}</p>
  ${renderPriceBlock()}
  ${renderOrderButtons(product)}
  <form id="order-form" data-name="${safeName}" data-nmid="${product.nmId}">
    <label>ФИО <input id="fio" required></label>
    <label>Телефон <input id="phone" type="tel" required></label>
    <label>Город и адрес доставки <input id="address" required></label>
    <button type="submit" data-target="whatsapp">Отправить в WhatsApp</button>
    <button type="submit" data-target="telegram">Отправить в Telegram</button>
  </form>
</article>`;
  return pageShell({
    title: product.name,
    description: truncate(product.description, 150),
    body,
    scripts: ['/order-form.js'],
  });
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `node --test test/render.test.js`
Expected: PASS (5 tests)

- [ ] **Step 5: Commit**

```bash
git add src/render.js test/render.test.js
git commit -m "Add index/product page HTML rendering"
```

---

### Task 6: Static assets — styles, search, order form

**Files:**
- Create: `static/style.css`
- Create: `static/search.js`
- Create: `static/order-form.js`

**Interfaces:**
- Consumes: `src/links.js` (Task 3) — imported directly by `order-form.js` at runtime in the browser (the build step, Task 8, copies `src/links.js` into `dist/links.js` unmodified).
- Produces: browser-side behavior only; no exported functions consumed by other tasks. Manually verified in a browser (Task 8's end-to-end check), not unit tested — DOM wiring is trivial glue over already-tested pure functions.

- [ ] **Step 1: Create `static/style.css`**

```css
body { font-family: system-ui, sans-serif; max-width: 1100px; margin: 0 auto; padding: 1rem; }
.grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(220px, 1fr)); gap: 1rem; }
.card { border: 1px solid #ddd; border-radius: 8px; padding: 0.75rem; }
.card img, .product img { width: 100%; height: auto; border-radius: 6px; }
.price-old { text-decoration: line-through; color: #888; margin-right: 0.5rem; }
.price-new { font-size: 1.3rem; font-weight: bold; color: #c0392b; }
.shipping { font-size: 0.85rem; color: #2e7d32; }
.order-buttons { display: flex; flex-wrap: wrap; gap: 0.4rem; margin-top: 0.5rem; }
.order-buttons a { padding: 0.4rem 0.6rem; border: 1px solid #333; border-radius: 6px; text-decoration: none; color: inherit; font-size: 0.85rem; }
#order-form { display: flex; flex-direction: column; gap: 0.5rem; max-width: 400px; margin-top: 1.5rem; }
#search { width: 100%; padding: 0.5rem; margin: 1rem 0; font-size: 1rem; }
```

- [ ] **Step 2: Create `static/search.js`**

```js
const input = document.getElementById('search');
const cards = document.querySelectorAll('.card');

input.addEventListener('input', () => {
  const q = input.value.trim().toLowerCase();
  for (const card of cards) {
    card.style.display = card.dataset.name.includes(q) ? '' : 'none';
  }
});
```

- [ ] **Step 3: Create `static/order-form.js`**

```js
import { buildOrderMessage, buildWhatsAppLink, buildTelegramLink } from './links.js';

const form = document.getElementById('order-form');

form.addEventListener('submit', (event) => {
  event.preventDefault();

  const fio = document.getElementById('fio').value.trim();
  const phone = document.getElementById('phone').value.trim();
  const address = document.getElementById('address').value.trim();

  if (!fio || !phone || !address) {
    alert('Заполните ФИО, телефон и адрес доставки');
    return;
  }

  const message = buildOrderMessage({
    name: form.dataset.name,
    nmId: form.dataset.nmid,
    fio, phone, address,
  });

  const target = event.submitter.dataset.target;
  if (target === 'whatsapp') {
    window.open(buildWhatsAppLink(message), '_blank');
  } else {
    navigator.clipboard.writeText(message).catch(() => {});
    alert('Сообщение скопировано — вставьте его первым сообщением в Telegram');
    window.open(buildTelegramLink(), '_blank');
  }
});
```

- [ ] **Step 4: Commit**

```bash
git add static/
git commit -m "Add site styles, search filter and order form scripts"
```

---

### Task 7: Sitemap and robots.txt

**Files:**
- Create: `src/sitemap.js`
- Test: `test/sitemap.test.js`

**Interfaces:**
- Consumes: same `Product[]` shape as Task 5.
- Produces: `renderSitemap(products: Product[], baseUrl: string): string`; `renderRobotsTxt(baseUrl: string): string`. Consumed by `build.js` (Task 8).

- [ ] **Step 1: Write the failing test**

```js
// test/sitemap.test.js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { renderSitemap, renderRobotsTxt } from '../src/sitemap.js';

const products = [
  { nmId: 1, name: 'A', description: '', photo: 'x' },
  { nmId: 2, name: 'B', description: '', photo: 'y' },
];

test('renderSitemap lists the homepage and every product page once each', () => {
  const xml = renderSitemap(products, 'https://example.github.io/catalog');
  assert.match(xml, /<\?xml version="1\.0" encoding="UTF-8"\?>/);
  assert.match(xml, /<loc>https:\/\/example\.github\.io\/catalog<\/loc>/);
  assert.match(xml, /<loc>https:\/\/example\.github\.io\/catalog\/tovar\/1\/<\/loc>/);
  assert.match(xml, /<loc>https:\/\/example\.github\.io\/catalog\/tovar\/2\/<\/loc>/);
});

test('renderRobotsTxt allows everything and points at the sitemap', () => {
  const txt = renderRobotsTxt('https://example.github.io/catalog');
  assert.match(txt, /User-agent: \*/);
  assert.match(txt, /Allow: \//);
  assert.match(txt, /Sitemap: https:\/\/example\.github\.io\/catalog\/sitemap\.xml/);
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `node --test test/sitemap.test.js`
Expected: FAIL — module not found.

- [ ] **Step 3: Implement `src/sitemap.js`**

```js
export function renderSitemap(products, baseUrl) {
  const urls = [baseUrl, ...products.map(p => `${baseUrl}/tovar/${p.nmId}/`)];
  const entries = urls.map(u => `  <url><loc>${u}</loc></url>`).join('\n');
  return `<?xml version="1.0" encoding="UTF-8"?>\n<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">\n${entries}\n</urlset>\n`;
}

export function renderRobotsTxt(baseUrl) {
  return `User-agent: *\nAllow: /\nSitemap: ${baseUrl}/sitemap.xml\n`;
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `node --test test/sitemap.test.js`
Expected: PASS (2 tests)

- [ ] **Step 5: Commit**

```bash
git add src/sitemap.js test/sitemap.test.js
git commit -m "Add sitemap.xml and robots.txt generation"
```

---

### Task 8: Build orchestrator (end to end)

**Files:**
- Create: `src/build.js`
- Test: `test/build.test.js`

**Interfaces:**
- Consumes: `fetchAllClockProducts` (Task 4), `renderIndexPage`/`renderProductPage` (Task 5), `renderSitemap`/`renderRobotsTxt` (Task 7).
- Produces: `buildSite({ wbToken, baseUrl, outDir, fetchFn? }): Promise<number>` — writes the full site to `outDir`, returns the number of products included. Also runnable as a CLI (`node src/build.js`) reading config from environment variables, used by the GitHub Actions workflow (Task 9).

- [ ] **Step 1: Write the failing end-to-end test**

Uses one injected `fetchFn` for the WB API, and writes to a real temp directory.

```js
// test/build.test.js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { mkdtemp, readFile, rm } from 'node:fs/promises';
import { tmpdir } from 'node:os';
import path from 'node:path';
import { buildSite } from '../src/build.js';

test('buildSite writes index, one page per ready+photographed product, sitemap and robots.txt', async () => {
  const outDir = await mkdtemp(path.join(tmpdir(), 'catalog-build-'));

  const fetchFn = async (url) => {
    if (url.includes('content-api.wildberries.ru')) {
      return new Response(JSON.stringify({
        cards: [
          { nmID: 1, title: 'Часы А', description: 'Описание А', updatedAt: '2026-08-01T00:00:00Z',
            photos: [{ big: 'https://basket-46.wbbasket.ru/1/images/big/1.webp' }] },
          { nmID: 2, title: 'Часы Б', description: 'Описание Б', updatedAt: '2026-08-02T00:00:00Z',
            photos: [] }, // no photo -> excluded
        ],
      }), { status: 200 });
    }
    throw new Error(`unexpected fetch: ${url}`);
  };

  const count = await buildSite({
    wbToken: 't',
    baseUrl: 'https://example.github.io/catalog',
    outDir,
    fetchFn,
  });

  assert.equal(count, 1);

  const index = await readFile(path.join(outDir, 'index.html'), 'utf8');
  assert.match(index, /Часы А/);
  assert.doesNotMatch(index, /Часы Б/);

  const productPage = await readFile(path.join(outDir, 'tovar', '1', 'index.html'), 'utf8');
  assert.match(productPage, /Часы А/);

  await assert.rejects(() => readFile(path.join(outDir, 'tovar', '2', 'index.html')));

  const sitemap = await readFile(path.join(outDir, 'sitemap.xml'), 'utf8');
  assert.match(sitemap, /tovar\/1\//);

  const robots = await readFile(path.join(outDir, 'robots.txt'), 'utf8');
  assert.match(robots, /Sitemap:/);

  await readFile(path.join(outDir, 'links.js'), 'utf8'); // copied for the browser
  await readFile(path.join(outDir, 'style.css'), 'utf8');
  await readFile(path.join(outDir, 'search.js'), 'utf8');
  await readFile(path.join(outDir, 'order-form.js'), 'utf8');

  await rm(outDir, { recursive: true, force: true });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `node --test test/build.test.js`
Expected: FAIL — module not found.

- [ ] **Step 3: Implement `src/build.js`**

```js
import { mkdir, writeFile, copyFile } from 'node:fs/promises';
import path from 'node:path';
import { pathToFileURL } from 'node:url';
import { fetchAllClockProducts } from './wbCatalog.js';
import { renderIndexPage, renderProductPage } from './render.js';
import { renderSitemap, renderRobotsTxt } from './sitemap.js';

export async function buildSite({ wbToken, baseUrl, outDir, fetchFn = fetch }) {
  const products = await fetchAllClockProducts(wbToken, { fetchFn });

  await mkdir(outDir, { recursive: true });
  await writeFile(path.join(outDir, 'index.html'), renderIndexPage(products));
  await writeFile(path.join(outDir, 'sitemap.xml'), renderSitemap(products, baseUrl));
  await writeFile(path.join(outDir, 'robots.txt'), renderRobotsTxt(baseUrl));

  for (const product of products) {
    const productDir = path.join(outDir, 'tovar', String(product.nmId));
    await mkdir(productDir, { recursive: true });
    await writeFile(path.join(productDir, 'index.html'), renderProductPage(product));
  }

  await copyFile(new URL('./links.js', import.meta.url), path.join(outDir, 'links.js'));
  await copyFile(new URL('../static/style.css', import.meta.url), path.join(outDir, 'style.css'));
  await copyFile(new URL('../static/search.js', import.meta.url), path.join(outDir, 'search.js'));
  await copyFile(new URL('../static/order-form.js', import.meta.url), path.join(outDir, 'order-form.js'));

  return products.length;
}

async function main() {
  const count = await buildSite({
    wbToken: process.env.WB_API_TOKEN,
    baseUrl: process.env.SITE_BASE_URL,
    outDir: 'dist',
  });
  console.log(`Built ${count} products into dist/`);
}

if (import.meta.url === pathToFileURL(process.argv[1]).href) {
  main().catch((err) => {
    console.error(err);
    process.exitCode = 1;
  });
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `node --test test/build.test.js`
Expected: PASS (1 test)

- [ ] **Step 5: Run the full test suite**

Run: `node --test`
Expected: all tests across all files PASS.

- [ ] **Step 6: Commit**

```bash
git add src/build.js test/build.test.js
git commit -m "Add build orchestrator producing the full static site"
```

---

### Task 9: GitHub Actions workflow

**Files:**
- Create: `.github/workflows/build-deploy.yml`

**Interfaces:**
- Consumes: `npm test` (via `node --test`) and `node src/build.js` (Task 8) — the workflow's only job is to run these with the right environment/secrets and publish `dist/`.
- Requires repository secret `WB_API_TOKEN` and a repository variable `SITE_BASE_URL` — set up in Task 10, not committed anywhere.

- [ ] **Step 1: Create `.github/workflows/build-deploy.yml`**

```yaml
name: Build and deploy catalog

on:
  schedule:
    - cron: '0 3 * * *'
  workflow_dispatch: {}
  push:
    branches: [master]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: Run tests
        run: node --test
      - name: Build site
        run: node src/build.js
        env:
          WB_API_TOKEN: ${{ secrets.WB_API_TOKEN }}
          SITE_BASE_URL: ${{ vars.SITE_BASE_URL }}
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
```

- [ ] **Step 2: Commit**

```bash
git add .github/workflows/build-deploy.yml
git commit -m "Add GitHub Actions workflow: daily build + deploy to GitHub Pages"
```

(This workflow cannot succeed until Task 10 creates the repo, secrets and enables Pages — that's expected at this point.)

---

### Task 10: GitHub account, repository, secrets, and first live deploy

Not code — guided setup steps, done together with the user.

- [ ] **Step 1: GitHub account**

If the user doesn't have one: go to github.com/signup, create a free account. Note the chosen username — it fixes the site's URL (`https://<username>.github.io/<repo>/`).

- [ ] **Step 2: Create the repository**

On github.com, create a new **public** repository (private repos can't use free GitHub Pages) named e.g. `wall-clocks-catalog`. Do not initialize it with a README (this local repo already has commits).

- [ ] **Step 3: Push the local repo**

```bash
git remote add origin https://github.com/<username>/wall-clocks-catalog.git
git push -u origin master
```

- [ ] **Step 4: Add repository secrets and variable**

In the GitHub repo: Settings → Secrets and variables → Actions.
- Secret `WB_API_TOKEN` = the user's WB Content API token (the only secret).
- Variable (not secret) `SITE_BASE_URL` = `https://<username>.github.io/wall-clocks-catalog` (no trailing slash).

- [ ] **Step 5: Enable GitHub Pages**

Settings → Pages → Source: **GitHub Actions** (not "Deploy from a branch").

- [ ] **Step 6: Trigger the first run and verify**

Actions tab → "Build and deploy catalog" → Run workflow (uses `workflow_dispatch`). Wait for it to go green, then open `https://<username>.github.io/wall-clocks-catalog/` and confirm: products show with real WB photos, search box filters by typing, a product page opens with working Call/WhatsApp/Telegram/Buy-on-WB buttons and the order form opens WhatsApp/Telegram with a pre-filled message.

- [ ] **Step 7: Confirm the daily schedule**

No action needed — `schedule: cron: '0 3 * * *'` in the workflow (Task 9) now runs automatically every day. Note: GitHub may delay scheduled runs by minutes during high load; this is normal and fine for a once-a-day refresh.

---

## Self-Review Notes

- **Spec coverage:** data sources (Task 1, 4), inclusion rule (Task 2, verified in Task 8's test), card + product-page content and pricing (Task 3, 5), order buttons (Task 5), order form (Task 6), static pre-rendered HTML + per-product SEO pages + sitemap/robots (Task 5, 7), build/deploy automation (Task 8, 9), initial setup (Task 10). CDEK/Yandex pickup widgets are explicitly out of scope per the spec's "Отложено на потом" note — no task builds them.
- **Type consistency:** `Product` shape `{ nmId, name, description, photo }` is identical across Task 2 (stub, missing `photo`), Task 4 (supplies the `nmId → photo` map), Task 5/7 (consume the merged shape) — `build.js` (Task 8) is the single place that merges them, matching every other task's assumption.
- **No placeholders:** the one genuinely unknown detail (exact WB `cards/list` response field names) is resolved by a mandatory live-verification step (Task 4, Step 1) before any code is written against it, not left as a TODO.
